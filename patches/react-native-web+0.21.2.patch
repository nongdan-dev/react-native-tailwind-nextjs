diff --git a/node_modules/react-native-web/dist/exports/Pressable/index.js b/node_modules/react-native-web/dist/exports/Pressable/index.js
index d3148da..ee719f0 100644
--- a/node_modules/react-native-web/dist/exports/Pressable/index.js
+++ b/node_modules/react-native-web/dist/exports/Pressable/index.js
@@ -17,7 +17,6 @@ import { forwardRef, memo, useMemo, useState, useRef } from 'react';
 import useMergeRefs from '../../modules/useMergeRefs';
 import useHover from '../../modules/useHover';
 import usePressEvents from '../../modules/usePressEvents';
-import StyleSheet from '../StyleSheet';
 import View from '../View';
 /**
  * Component used to build display components that should respond to whether the
@@ -128,7 +127,13 @@ function Pressable(props, forwardedRef) {
     onFocus: focusHandler,
     onKeyDown: keyDownHandler,
     ref: setRef,
-    style: [disabled ? styles.disabled : styles.active, typeof style === 'function' ? style(interactionState) : style],
+    style,
+    className: props.className,
+    __rnwClassNameData: Object.assign({
+      Pressable: {
+        disabled,
+      },
+    }, props.__rnwClassNameData),
     tabIndex: _tabIndex
   }), typeof children === 'function' ? children(interactionState) : children);
 }
@@ -138,15 +143,6 @@ function useForceableState(forced) {
     setBool = _useState[1];
   return [bool || forced, setBool];
 }
-var styles = StyleSheet.create({
-  active: {
-    cursor: 'pointer',
-    touchAction: 'manipulation'
-  },
-  disabled: {
-    pointerEvents: 'box-none'
-  }
-});
 var MemoedPressable = /*#__PURE__*/memo(/*#__PURE__*/forwardRef(Pressable));
 MemoedPressable.displayName = 'Pressable';
 export default MemoedPressable;
diff --git a/node_modules/react-native-web/dist/exports/ScrollView/index.js b/node_modules/react-native-web/dist/exports/ScrollView/index.js
index c4f9b5b..e91c938 100644
--- a/node_modules/react-native-web/dist/exports/ScrollView/index.js
+++ b/node_modules/react-native-web/dist/exports/ScrollView/index.js
@@ -20,7 +20,6 @@ import invariant from 'fbjs/lib/invariant';
 import mergeRefs from '../../modules/mergeRefs';
 import Platform from '../Platform';
 import ScrollViewBase from './ScrollViewBase';
-import StyleSheet from '../StyleSheet';
 import TextInputState from '../../modules/TextInputState';
 import UIManager from '../UIManager';
 import View from '../View';
@@ -543,11 +542,6 @@ class ScrollView extends React.Component {
       onScroll = _this$props.onScroll,
       centerContent = _this$props.centerContent,
       other = _objectWithoutPropertiesLoose(_this$props, _excluded);
-    if (process.env.NODE_ENV !== 'production' && this.props.style) {
-      var style = StyleSheet.flatten(this.props.style);
-      var childLayoutProps = ['alignItems', 'justifyContent'].filter(prop => style && style[prop] !== undefined);
-      invariant(childLayoutProps.length === 0, "ScrollView child layout (" + JSON.stringify(childLayoutProps) + ") " + 'must be applied through the contentContainerStyle prop.');
-    }
     var contentSizeChangeProps = {};
     if (onContentSizeChange) {
       contentSizeChangeProps = {
@@ -559,7 +553,12 @@ class ScrollView extends React.Component {
       var isSticky = hasStickyHeaderIndices && stickyHeaderIndices.indexOf(i) > -1;
       if (child != null && (isSticky || pagingEnabled)) {
         return /*#__PURE__*/React.createElement(View, {
-          style: [isSticky && styles.stickyHeader, pagingEnabled && styles.pagingEnabledChild]
+          __rnwClassNameData: {
+            ScrollView: {
+              stickyHeader: isSticky,
+              pagingEnabledChild: pagingEnabled,
+            },
+          },
         }, child);
       } else {
         return child;
@@ -569,12 +568,27 @@ class ScrollView extends React.Component {
       children: children,
       collapsable: false,
       ref: this._setInnerViewRef,
-      style: [horizontal && styles.contentContainerHorizontal, centerContent && styles.contentContainerCenterContent, contentContainerStyle]
+      style: contentContainerStyle,
+      className: this.props.contentContainerClassName,
+      __rnwClassNameData: {
+        ScrollView: {
+          pagingEnabledHorizontal: horizontal,
+          contentContainerCenterContent: centerContent,
+        },
+      },
     }));
-    var baseStyle = horizontal ? styles.baseHorizontal : styles.baseVertical;
-    var pagingEnabledStyle = horizontal ? styles.pagingEnabledHorizontal : styles.pagingEnabledVertical;
     var props = _objectSpread(_objectSpread({}, other), {}, {
-      style: [baseStyle, pagingEnabled && pagingEnabledStyle, this.props.style],
+      style: this.props.style,
+      className: this.props.className,
+      __rnwClassNameData: Object.assign({
+        ScrollView: {
+          base: true,
+          baseHorizontal: horizontal,
+          baseVertical: !horizontal,
+          pagingEnabledHorizontal: horizontal,
+          pagingEnabledVertical: !horizontal,
+        },
+      }, this.props.__rnwClassNameData),
       onTouchStart: this.scrollResponderHandleTouchStart,
       onTouchMove: this.scrollResponderHandleTouchMove,
       onTouchEnd: this.scrollResponderHandleTouchEnd,
@@ -599,55 +613,14 @@ class ScrollView extends React.Component {
     }), contentContainer);
     if (refreshControl) {
       return /*#__PURE__*/React.cloneElement(refreshControl, {
-        style: props.style
+        style: props.style,
+        className: props.className,
+        __rnwClassNameData: props.__rnwClassNameData,
       }, scrollView);
     }
     return scrollView;
   }
 }
-var commonStyle = {
-  flexGrow: 1,
-  flexShrink: 1,
-  // Enable hardware compositing in modern browsers.
-  // Creates a new layer with its own backing surface that can significantly
-  // improve scroll performance.
-  transform: 'translateZ(0)',
-  // iOS native scrolling
-  WebkitOverflowScrolling: 'touch'
-};
-var styles = StyleSheet.create({
-  baseVertical: _objectSpread(_objectSpread({}, commonStyle), {}, {
-    flexDirection: 'column',
-    overflowX: 'hidden',
-    overflowY: 'auto'
-  }),
-  baseHorizontal: _objectSpread(_objectSpread({}, commonStyle), {}, {
-    flexDirection: 'row',
-    overflowX: 'auto',
-    overflowY: 'hidden'
-  }),
-  contentContainerHorizontal: {
-    flexDirection: 'row'
-  },
-  contentContainerCenterContent: {
-    justifyContent: 'center',
-    flexGrow: 1
-  },
-  stickyHeader: {
-    position: 'sticky',
-    top: 0,
-    zIndex: 10
-  },
-  pagingEnabledHorizontal: {
-    scrollSnapType: 'x mandatory'
-  },
-  pagingEnabledVertical: {
-    scrollSnapType: 'y mandatory'
-  },
-  pagingEnabledChild: {
-    scrollSnapAlign: 'start'
-  }
-});
 var ForwardedScrollView = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
   return /*#__PURE__*/React.createElement(ScrollView, _extends({}, props, {
     forwardedRef: forwardedRef
diff --git a/node_modules/react-native-web/dist/exports/Text/index.js b/node_modules/react-native-web/dist/exports/Text/index.js
index 1076f55..01ac398 100644
--- a/node_modules/react-native-web/dist/exports/Text/index.js
+++ b/node_modules/react-native-web/dist/exports/Text/index.js
@@ -21,7 +21,6 @@ import useElementLayout from '../../modules/useElementLayout';
 import useMergeRefs from '../../modules/useMergeRefs';
 import usePlatformMethods from '../../modules/usePlatformMethods';
 import useResponderEvents from '../../modules/useResponderEvents';
-import StyleSheet from '../StyleSheet';
 import TextAncestorContext from './TextAncestorContext';
 import { useLocaleContext, getLocaleDirection } from '../../modules/useLocale';
 //import { warnOnce } from '../../modules/warnOnce';
@@ -110,9 +109,16 @@ var Text = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
   if (onClick || onPress) {
     supportedProps.onClick = handleClick;
   }
-  supportedProps.style = [numberOfLines != null && numberOfLines > 1 && {
-    WebkitLineClamp: numberOfLines
-  }, hasTextAncestor === true ? styles.textHasAncestor$raw : styles.text$raw, numberOfLines === 1 && styles.textOneLine, numberOfLines != null && numberOfLines > 1 && styles.textMultiLine, props.style, selectable === true && styles.selectable, selectable === false && styles.notSelectable, onPress && styles.pressable];
+  supportedProps.style = props.style;
+  supportedProps.className = props.className;
+  supportedProps.__rnwClassNameData = Object.assign({
+    Text: {
+      hasTextAncestor,
+      numberOfLines,
+      selectable,
+      pressable: !!onPress,
+    },
+  }, props.__rnwClassNameData);
   if (props.href != null) {
     component = 'a';
     if (hrefAttrs != null) {
@@ -141,53 +147,4 @@ var Text = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
   }, element);
 });
 Text.displayName = 'Text';
-var textStyle = {
-  backgroundColor: 'transparent',
-  border: '0 solid black',
-  boxSizing: 'border-box',
-  color: 'black',
-  display: 'inline',
-  font: '14px System',
-  listStyle: 'none',
-  margin: 0,
-  padding: 0,
-  position: 'relative',
-  textAlign: 'start',
-  textDecoration: 'none',
-  whiteSpace: 'pre-wrap',
-  wordWrap: 'break-word'
-};
-var styles = StyleSheet.create({
-  text$raw: textStyle,
-  textHasAncestor$raw: _objectSpread(_objectSpread({}, textStyle), {}, {
-    color: 'inherit',
-    font: 'inherit',
-    textAlign: 'inherit',
-    whiteSpace: 'inherit'
-  }),
-  textOneLine: {
-    maxWidth: '100%',
-    overflow: 'hidden',
-    textOverflow: 'ellipsis',
-    whiteSpace: 'nowrap',
-    wordWrap: 'normal'
-  },
-  // See #13
-  textMultiLine: {
-    display: '-webkit-box',
-    maxWidth: '100%',
-    overflow: 'clip',
-    textOverflow: 'ellipsis',
-    WebkitBoxOrient: 'vertical'
-  },
-  notSelectable: {
-    userSelect: 'none'
-  },
-  selectable: {
-    userSelect: 'text'
-  },
-  pressable: {
-    cursor: 'pointer'
-  }
-});
 export default Text;
diff --git a/node_modules/react-native-web/dist/exports/TextInput/index.js b/node_modules/react-native-web/dist/exports/TextInput/index.js
index 0f476a7..605fb92 100644
--- a/node_modules/react-native-web/dist/exports/TextInput/index.js
+++ b/node_modules/react-native-web/dist/exports/TextInput/index.js
@@ -20,7 +20,6 @@ import useMergeRefs from '../../modules/useMergeRefs';
 import usePlatformMethods from '../../modules/usePlatformMethods';
 import useResponderEvents from '../../modules/useResponderEvents';
 import { getLocaleDirection, useLocaleContext } from '../../modules/useLocale';
-import StyleSheet from '../StyleSheet';
 import TextInputState from '../../modules/TextInputState';
 //import { warnOnce } from '../../modules/warnOnce';

@@ -376,9 +375,14 @@ var TextInput = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
   */
   supportedProps.rows = multiline ? rows != null ? rows : numberOfLines : 1;
   supportedProps.spellCheck = spellCheck != null ? spellCheck : autoCorrect;
-  supportedProps.style = [{
-    '--placeholderTextColor': placeholderTextColor
-  }, styles.textinput$raw, styles.placeholder, props.style, caretHidden && styles.caretHidden];
+  supportedProps.style = props.style;
+  supportedProps.className = props.className;
+  supportedProps.__rnwClassNameData = Object.assign({
+    TextInput: {
+      placeholderTextColor,
+      caretHidden,
+    },
+  }, props.__rnwClassNameData);
   supportedProps.type = multiline ? undefined : type;
   supportedProps.virtualkeyboardpolicy = showSoftInputOnFocus === false ? 'manual' : 'auto';
   var platformMethodsRef = usePlatformMethods(supportedProps);
@@ -395,24 +399,4 @@ var TextInput = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
 TextInput.displayName = 'TextInput';
 // $FlowFixMe
 TextInput.State = TextInputState;
-var styles = StyleSheet.create({
-  textinput$raw: {
-    MozAppearance: 'textfield',
-    WebkitAppearance: 'none',
-    backgroundColor: 'transparent',
-    border: '0 solid black',
-    borderRadius: 0,
-    boxSizing: 'border-box',
-    font: '14px System',
-    margin: 0,
-    padding: 0,
-    resize: 'none'
-  },
-  placeholder: {
-    placeholderTextColor: 'var(--placeholderTextColor)'
-  },
-  caretHidden: {
-    caretColor: 'transparent'
-  }
-});
 export default TextInput;
diff --git a/node_modules/react-native-web/dist/exports/View/index.js b/node_modules/react-native-web/dist/exports/View/index.js
index 45343e5..2347bb5 100644
--- a/node_modules/react-native-web/dist/exports/View/index.js
+++ b/node_modules/react-native-web/dist/exports/View/index.js
@@ -20,7 +20,6 @@ import useElementLayout from '../../modules/useElementLayout';
 import useMergeRefs from '../../modules/useMergeRefs';
 import usePlatformMethods from '../../modules/usePlatformMethods';
 import useResponderEvents from '../../modules/useResponderEvents';
-import StyleSheet from '../StyleSheet';
 import TextAncestorContext from '../Text/TextAncestorContext';
 import { useLocaleContext, getLocaleDirection } from '../../modules/useLocale';
 var forwardPropsList = Object.assign({}, forwardedProps.defaultProps, forwardedProps.accessibilityProps, forwardedProps.clickProps, forwardedProps.focusProps, forwardedProps.keyboardProps, forwardedProps.mouseProps, forwardedProps.touchProps, forwardedProps.styleProps, {
@@ -87,7 +86,13 @@ var View = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
   var writingDirection = componentDirection || contextDirection;
   var supportedProps = pickProps(rest);
   supportedProps.dir = componentDirection;
-  supportedProps.style = [styles.view$raw, hasTextAncestor && styles.inline, props.style];
+  supportedProps.style = props.style;
+  supportedProps.className = props.className;
+  supportedProps.__rnwClassNameData = Object.assign({
+    View: {
+      inline: hasTextAncestor,
+    },
+  }, props.__rnwClassNameData);
   if (props.href != null) {
     component = 'a';
     if (hrefAttrs != null) {
@@ -113,28 +118,4 @@ var View = /*#__PURE__*/React.forwardRef((props, forwardedRef) => {
   });
 });
 View.displayName = 'View';
-var styles = StyleSheet.create({
-  view$raw: {
-    alignContent: 'flex-start',
-    alignItems: 'stretch',
-    backgroundColor: 'transparent',
-    border: '0 solid black',
-    boxSizing: 'border-box',
-    display: 'flex',
-    flexBasis: 'auto',
-    flexDirection: 'column',
-    flexShrink: 0,
-    listStyle: 'none',
-    margin: 0,
-    minHeight: 0,
-    minWidth: 0,
-    padding: 0,
-    position: 'relative',
-    textDecoration: 'none',
-    zIndex: 0
-  },
-  inline: {
-    display: 'inline-flex'
-  }
-});
 export default View;
diff --git a/node_modules/react-native-web/dist/modules/createDOMProps/index.js b/node_modules/react-native-web/dist/modules/createDOMProps/index.js
index 18f5e8b..3ded080 100644
--- a/node_modules/react-native-web/dist/modules/createDOMProps/index.js
+++ b/node_modules/react-native-web/dist/modules/createDOMProps/index.js
@@ -809,8 +809,26 @@ var createDOMProps = (elementType, props, options) => {
     }, options)),
     className = _StyleSheet[0],
     inlineStyle = _StyleSheet[1];
-  if (className) {
-    domProps.className = className;
+  if (_props.__rnwClassNameData) {
+    var o = Object.assign(
+      {
+        className: _props.className,
+        pointerEvents,
+        writingDirection: 'ltr',
+      },
+      options,
+    );
+    var f;
+    if (typeof window === 'object' && window) {
+      f = window.__rnwClassName;
+    }
+    if (typeof global === 'object' && global) {
+      f = global.__rnwClassName;
+    }
+    domProps.className = f(_props.__rnwClassNameData, o);
+    delete domProps.__rnwClassNameData;
+  } else if (className) {
+    domProps.className = [className, domProps.className].filter(v => v).join(' ');
   }
   if (inlineStyle) {
     domProps.style = inlineStyle;
diff --git a/node_modules/react-native-web/dist/modules/forwardedProps/index.js b/node_modules/react-native-web/dist/modules/forwardedProps/index.js
index 2e60013..76455e1 100644
--- a/node_modules/react-native-web/dist/modules/forwardedProps/index.js
+++ b/node_modules/react-native-web/dist/modules/forwardedProps/index.js
@@ -8,6 +8,8 @@
  */

 export var defaultProps = {
+  __rnwClassNameData: true,
+  className: true,
   children: true,
   dataSet: true,
   dir: true,
diff --git a/node_modules/react-native-web/dist/vendor/react-native/FlatList/index.js b/node_modules/react-native-web/dist/vendor/react-native/FlatList/index.js
index 86af896..08c9423 100644
--- a/node_modules/react-native-web/dist/vendor/react-native/FlatList/index.js
+++ b/node_modules/react-native-web/dist/vendor/react-native/FlatList/index.js
@@ -13,7 +13,6 @@ var _excluded = ["numColumns", "columnWrapperStyle", "removeClippedSubviews", "s
  */

 import View from '../../../exports/View';
-import StyleSheet from '../../../exports/StyleSheet';
 import deepDiffer from '../deepDiffer';
 import Platform from '../../../exports/Platform';
 import invariant from 'fbjs/lib/invariant';
@@ -311,7 +310,13 @@ class FlatList extends React.PureComponent {
             _index = info.index;
           invariant(Array.isArray(_item2), 'Expected array of items with numColumns > 1');
           return /*#__PURE__*/React.createElement(View, {
-            style: [styles.row, columnWrapperStyle]
+            style: columnWrapperStyle,
+            className: this.props.columnWrapperClassName,
+            __rnwClassNameData: Object.assign({
+              FlatList: {
+                columnWrapper: true,
+              },
+            }, this.props.__rnwClassNameData),
           }, _item2.map((it, kk) => {
             var element = render({
               // $FlowFixMe[incompatible-call]
@@ -437,9 +442,4 @@ class FlatList extends React.PureComponent {
     );
   }
 }
-var styles = StyleSheet.create({
-  row: {
-    flexDirection: 'row'
-  }
-});
 export default FlatList;

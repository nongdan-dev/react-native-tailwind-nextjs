import { tw } from '@/tw'

// use tw`` here to collect and map when class names are minified
// these class names should match with custom variant in global.css
export const cnDark = tw`dark`
export const cnLight = tw`light`
